server:
  port: 18082
spring:
  application:
    name: user-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
feign:
  hystrix:
    enabled: true
#  compression:
#      request:
#        enabled: true # 开启请求压缩
#      response:
#        enabled: true # 开启响应压缩
logging:
  level:
    com.itheima: debug